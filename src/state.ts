import { reactive } from 'vue';

export default reactive({
    hyperbloomui: {
        theme: {
            name: 'debug-theme',
            colors: {
                accent: 'black'
            }
        }
    }
});
